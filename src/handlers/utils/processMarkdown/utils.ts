export const codeWithoutLanguage =
  '⚠️ Foi encontrado trechos de código sem a linguagem especificada, especifique uma linguagem para a formatação ficar mais bonita.';

export const requestJsonWithoutHeaderMessage = `⚠️ A request não continha um header indicando que é um JSON como \`Content-Type: application/json\`, como foi encontrado um JSON referente a request, o header será aplicado`;
